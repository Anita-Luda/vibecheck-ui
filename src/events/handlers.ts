import { EventBus } from '../runtime/eventBus';
import { E } from '../../contracts/events';

export const registerHandlers = (bus: EventBus) => {
    // Handlers are registered in main.tsx or executor.ts
    // This file serves as the registry for complex event logic
};
